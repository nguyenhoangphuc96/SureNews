<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_decrease_size"
        android:icon="@drawable/ic_size_decrease"
        android:title="decrease"
        app:showAsAction="always" />
    <item android:id="@+id/menu_default_size"
        android:icon="@drawable/ic_size_default"
        android:title="decrease"
        app:showAsAction="always" />
    <item android:id="@+id/menu_increase_size"
        android:icon="@drawable/ic_size_increase"
        android:title="decrease"
        app:showAsAction="always" />
</menu>